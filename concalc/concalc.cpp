#include "concalc.h"

ConCalc::ConCalc (std::string strInput){
    for (int i = 0; i <= strInput.length(); i++){
        //if strInput[i] != '+'
    }

//    // loop through input (string)
//    // parse types and values for evaluation
//    int strLen = sInput.length();
//
//
//    for (int i = 0; i <= strLen; i++){
//        switch (sInput[i]){
//            case '+' || '-' || '*' || '/':
//                cOperator = sInput[i];
//        }
//    }
}

ConCalc::~ConCalc(){
//dcon
}

// Ideally, this will be the only method
// called by main.
void ConCalc::calculate(std::string stringIn){
    // parser sets variables (numbers, operator)
    // here, calculate while referring to these
    // members
}
